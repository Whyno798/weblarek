# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

##### Данные
В приложении используются две сущности данных: товар (IProduct) и покупатель (IBuyer). Эти интерфейсы определяют структуру данных для католога, корзины и оформления заказа.

###### Интерфейс IProduct
Интерфейс описывает свойства товара, доступного для покупки.

interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

Этот интерфейс испрользуется для хранения и передачи информации о товарах в катологи и корзине.

###### Интерфейс IBuyer
Интерфейс определяет данные покупателя, необходимые для оформления заказа.

interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
}

TPayment — это тип, определяющий возможные способы оплаты, который должен быть объявлен отдельно.

###### Модели данных

Работа с данными реализована через три класса: Catalog (каталог товаров), Cart (корзина) и Buyer (покупатель). Каждый класс инкапсулирует свою зону ответственности, реализуя методы для хранения, получения и манипуляции данными.
​
###### Класс Catalog
Назначение: Хранение всех доступных товаров и управление выделенным товаром для детального просмотра.

constructor()
Не принимает параметров, инициализирует пустые коллекции товаров и выделенного товара.

Поля:
`products: IProduct[]` — массив всех товаров из каталога.
`selectedProduct: IProduct | null` — товар, выбранный для подробного отображения.

Методы:
`saveProducts(products: IProduct[]): void` — сохраняет переданный массив товаров в поле products.
`getProducts(): IProduct[]` — возвращает массив всех товаров.
`getProductById(id: string): IProduct | undefined` — возвращает товар по его ID или undefined, если не найден.
`setSelectedProduct(product: IProduct): void` — сохраняет товар для детального отображения.
`getSelectedProduct(): IProduct | null` — возвращает выделенный товар или null

###### Класс Cart
Назначение: Управление товарами, добавленными в корзину покупателем.

Конструктор:

constructor()
Не принимает параметров, инициализирует пустой массив товаров.

Поля:
`items: IProduct[]` — массив товаров в корзине.

Методы:
`getItems(): IProduct[]` — возвращает массив товаров в корзине.
`addItem(product: IProduct): void` — добавляет переданный товар в корзину.
`removeItem(product: IProduct): void` — удаляет переданный товар из корзины.
`clear(): void` — очищает корзину, удаляя все товары.
`getTotalPrice(): number` — возвращает общую стоимость всех товаров в корзине.
`getItemCount(): number` — возвращает количество товаров в корзине.
`hasItem(id: string): boolean` — проверяет наличие товара с заданным ID в корзине.
​
###### Класс Buyer
Назначение: Хранение и валидация данных покупателя для оформления заказа.

Конструктор:

constructor()
Не принимает параметров, инициализирует пустые поля данных.

Поля:
`payment: TPayment | null` — вид оплаты.
`email: string` — email покупателя.
`phone: string` — телефон покупателя.
`address: string` — адрес доставки.

Методы:
`setPayment(payment: TPayment): void` — сохраняет вид оплаты, не затрагивая другие поля.
`setEmail(email: string): void` — сохраняет email, не затрагивая другие поля.
`setPhone(phone: string): void` — сохраняет телефон, не затрагивая другие поля.
`setAddress(address: string): void` — сохраняет адрес, не затрагивая другие поля.
`getData(): IBuyer` — возвращает объект с текущими данными покупателя.
`clear(): void` — очищает все поля данных.
`validate(): Record<string, string> | null` — возвращает объект с ошибками валидации (ключ — поле, значение — сообщение об ошибке) или null, если все поля валидны. Поле валидно, если не пустое​.

###### Слой коммуникации

###### Класс ApiService 
Обеспечивает взаимодействие приложения с API "Веб-ларек" через композицию с классом Api.

Конструктор
constructor(api: IApi)
api:IApi - экземпляр класса Api для выполнения HTTP-запросов.

Поля:
prvate api: IApi - сервиc для выполнения HTTP-запросов к серверу.

Методы:
`async getProducts(): Promise<{ items: IProduct[] }>` - Выполняет GET-запрос на /product/. Возвращает объект { items: IProduct[] } с массивом товаров от сервера.

`async createOrder(orderData: { items: IProduct[], buyer: IBuyer }): Promise<{ total: number }>` - Выполняет POST-запрос на /order/ с данными заказа. Параметры: orderData — объект с товарами из корзины и данными покупателя. Возвращает объект { total: number } — подтверждение суммы заказа.

###### Слой представления
Отвечает за отображение интерфейса приложения и взаимодействие с пользователем. Каждый класс представления работает только со своим DOM-блоком, не содержит бизнес-логики и при действиях пользователя генерирует события, которые затем обрабатывает презентер.

###### Класс Header
Отвечает за отображение верхней части страницы сайта. Управляет кнопкой открытия корзины и счетчиком товаров в корзине, показывая пользователю текущее состояние заказа.

Конструктор:
`сonstructor(container: HTMLElement, event: IEvents)` - принимает корневой DOM-элемент шапки сайта и экземпляр брокера событий для обработки пользовательских действий.

Поля:
`container: HTMLElement` — корневой элемент шапки сайта.
`events: IEvents` — экземпляр брокера событий.
`basketButton: HTMLButtonElement` — кнопка открытия корзины.
`counterElement: HTMLElement` — элемент для отображения количества товаров в корзине.

Методы:
`set counter(value: number)` — обновляет отображаемое количество товаров в корзине.
При нажатии на кнопку корзины генерирует событие открытия корзины, например basket:open.

###### Класс Gallery
Отвечает за отображение списка карточек товаров на главной странице. Он получает набор готовых DOM-элементов карточек и вставляет их в контейнер галереи.

Конструктор:
`constructor(container: HTMLElement)` — принимает корневой DOM-элемент галереи товаров.

Поля:
`container: HTMLElement` — элемент галереи, в который выводятся карточки товаров.

Методы:
`set items(items: HTMLElement[])` — заменяет содержимое галереи переданным массивом карточек товаров.
`render(): HTMLElement` — возвращает корневой DOM-элемент галереи.

###### Класс Modal
Отвечает за работу модального окна: открытие, закрытие и отображение переданного контента. От этого класса не наследуются другие классы, потому что любой контент внутри модального окна является самостоятельным компонентом представления.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает корневой DOM-элемент модального окна и экземпляр брокера событий.

Поля:
`container: HTMLElement` — корневой элемент модального окна.
`events: IEvents` — экземпляр брокера событий.
`closeButton: HTMLButtonElement` — кнопка закрытия модального окна.
`contentElement: HTMLElement` — контейнер для отображаемого содержимого модального окна.

Методы:
`set content(value: HTMLElement)` — устанавливает компонент, который должен быть показан внутри модального окна.
`open(): void` — открывает модальное окно и генерирует событие открытия модального окна.
`close(): void` — закрывает модальное окно и генерирует событие закрытия модального окна.
`render(): HTMLElement` — возвращает DOM-элемент модального окна.

###### Класс Card
Базовый класс для всех карточек товара. Содержит общий функционал для отображения данных товара, который используется в карточке каталога, карточке предпросмотра и карточке корзины.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает корневой DOM-элемент карточки и экземпляр брокера событий.

Поля:  
`container: HTMLElement` — корневой элемент карточки.
`events: IEvents` — экземпляр брокера событий.
`titleElement: HTMLElement` — элемент названия товара.
`priceElement: HTMLElement` — элемент цены товара.
`id: string` — идентификатор товара.

Методы:  
`set id(value: string)` — сохраняет идентификатор товара.
`set title(value: string)` — устанавливает название товара.
`set price(value: number | null)` — устанавливает цену товара или состояние недоступности покупки.
`render(data?: Partial<IProduct>): HTMLElement` — заполняет карточку переданными данными и возвращает DOM-элемент.

###### Класс CatalogCard
Наследник класса `Card`, отвечающий за отображение карточки товара в галерее каталога. Показывает краткую информацию о товаре и позволяет выбрать его для просмотра.

Конструктор:  
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент карточки каталога и экземпляр брокера событий.

Поля:
Наследует поля класса `Card`.
`imageElement: HTMLImageElement` — изображение товара.
`categoryElement: HTMLElement` — категория товара.

Методы:  
`set image(value: string)` — устанавливает изображение товара.
`set category(value: string)` — устанавливает категорию товара.
`render(data?: Partial<IProduct>): HTMLElement` — отображает карточку товара в каталоге.
При клике по карточке генерирует событие выбора товара, например `card:select`.

###### Класс PreviewCard
Наследник класса `Card`, отвечающий за отображение подробной информации о товаре. Используется для показа карточки товара в модальном окне.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент карточки предпросмотра и экземпляр брокера событий.

Поля:
Наследует поля класса `Card`.
`imageElement: HTMLImageElement` — изображение товара.
`categoryElement: HTMLElement` — категория товара.
`descriptionElement: HTMLElement` — описание товара.
`buttonElement: HTMLButtonElement` — кнопка добавления товара в корзину.

Методы:
`set image(value: string)` — устанавливает изображение товара.
`set category(value: string)` — устанавливает категорию товара.
`set description(value: string)` — устанавливает описание товара.
`set disabled(value: boolean)` — управляет доступностью кнопки покупки.
`render(data?: Partial<IProduct>): HTMLElement` — отображает подробную карточку товара.
При нажатии на кнопку генерирует событие добавления товара в корзину, например `card:add`.

###### Класс BasketCard
Наследник класса `Card`, отвечающий за отображение товара в корзине. Используется для вывода товара в списке выбранных позиций.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент карточки корзины и экземпляр брокера событий.

Поля:
Наследует поля класса `Card`.
`indexElement: HTMLElement` — элемент с порядковым номером товара в корзине.
`buttonElement: HTMLButtonElement` — кнопка удаления товара из корзины.

Методы:
`set index(value: number)` — устанавливает порядковый номер товара в списке.
`render(data?: Partial<IProduct>): HTMLElement` — отображает карточку товара в корзине.
При нажатии на кнопку удаления генерирует событие удаления товара, например `basket:remove`.

###### Класс Form
Базовый класс для всех форм приложения. Содержит общий функционал для обработки ввода, валидации, отображения ошибок и отправки формы.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — принимает корневой DOM-элемент формы и экземпляр брокера событий.

Поля:
`container: HTMLFormElement` — корневой элемент формы.
`events: IEvents` — экземпляр брокера событий.
`submitButton: HTMLButtonElement` — кнопка отправки формы.
`errorsElement: HTMLElement` — элемент отображения ошибок формы.

Методы:
`set valid(value: boolean)` — управляет доступностью кнопки отправки.
`set errors(value: string)` — отображает текст ошибок формы.
`render(state?: Partial<T>): HTMLElement` — возвращает DOM-элемент формы с обновленными данными.
При изменении полей формы генерирует события изменения данных.
При отправке формы генерирует событие отправки формы, например `form:submit`.

###### Класс OrderForm
Наследник класса `Form`, отвечающий за форму выбора способа оплаты и ввода адреса доставки.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — принимает DOM-элемент формы заказа и экземпляр брокера событий.

Поля:
Наследует поля класса `Form`.
`cardButton: HTMLButtonElement` — кнопка выбора оплаты картой.
`cashButton: HTMLButtonElement` — кнопка выбора оплаты наличными.
`addressInput: HTMLInputElement` — поле адреса доставки.

Методы:
`set payment(value: TPayment)` — отображает выбранный способ оплаты.
`set address(value: string)` — устанавливает значение поля адреса.
`render(data?: Partial<IBuyer>): HTMLElement` — отображает форму заказа.
При выборе способа оплаты генерирует событие, например `order.payment:change`.
При вводе адреса генерирует событие, например `order.address:change`.
При отправке формы генерирует событие перехода к следующему шагу, например `order:submit`.

###### Класс ContactsForm
Наследник класса `Form`, отвечающий за форму ввода контактных данных покупателя.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — принимает DOM-элемент формы контактов и экземпляр брокера событий.

Поля:
Наследует поля класса `Form`.
`emailInput: HTMLInputElement` — поле ввода email.
`phoneInput: HTMLInputElement` — поле ввода телефона.

Методы:
`set email(value: string)` — устанавливает значение email.
`set phone(value: string)` — устанавливает значение телефона.
`render(data?: Partial<IBuyer>): HTMLElement` — отображает форму контактов.
При изменении email генерирует событие, например `contacts.email:change`.
При изменении телефона генерирует событие, например `contacts.phone:change`.
При отправке формы генерирует событие завершения оформления, например `contacts:submit`.

###### Класс Basket
Отвечает за отображение содержимого корзины: списка товаров, общей стоимости заказа и кнопки перехода к оформлению.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает корневой DOM-элемент корзины и экземпляр брокера событий.

Поля:
`container: HTMLElement` — корневой элемент корзины.
`events: IEvents` — экземпляр брокера событий.
`listElement: HTMLElement` — контейнер списка товаров.
`totalElement: HTMLElement` — элемент общей стоимости.
`buttonElement: HTMLButtonElement` — кнопка перехода к оформлению заказа.

Методы:
`set items(items: HTMLElement[])` — отображает список товаров в корзине.
`set total(value: number)` — отображает итоговую стоимость заказа.
`set disabled(value: boolean)` — управляет доступностью кнопки оформления.
При нажатии на кнопку оформления генерирует событие, например `basket:submit`.

###### Класс Success
Отвечает за отображение сообщения об успешном оформлении заказа. Показывает итоговую сумму списания и кнопку закрытия.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает DOM-элемент сообщения об успешном заказе и экземпляр брокера событий.

Поля:
`container: HTMLElement` — корневой элемент компонента.
`events: IEvents` — экземпляр брокера событий.
`descriptionElement: HTMLElement` — текст с итоговой суммой заказа.
`buttonElement: HTMLButtonElement` — кнопка закрытия окна.

Методы:
`set total(value: number)` — отображает итоговую сумму заказа.
`render(data?: { total: number }): HTMLElement` — возвращает DOM-элемент компонента.
При нажатии на кнопку генерирует событие закрытия, например `success:close`.